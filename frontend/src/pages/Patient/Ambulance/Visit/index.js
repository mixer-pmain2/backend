import React from "react";
import {connect} from "react-redux";

import Layout from "../../../Layout";

const VisitPage =() => {
    return <Layout>

    </Layout>
}

export default connect((state) => ({}))(VisitPage)